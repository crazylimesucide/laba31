#include <iostream>
#include <cmath>
#include <math.h>
using namespace std;
int main()
{
	double x;
	double y;
	double A;
	double B;
	cout << "x = "; cin >> x;

	A = (x*x/(2,1+sin(abs(x))));

	if (x <= (-5))
		(cos(exp(x))/sin(exp(x)));
	if (-5 < x && x < 0)
		(2-(pow(x,3)/(abs(x)+1)));
	if (x >= 0)
		B = (log(sqrt(abs(x)-((x*x)/2))));
	y = A + B;
	cout << endl;
	cout << "1) y = " << y << endl;	return 0;}